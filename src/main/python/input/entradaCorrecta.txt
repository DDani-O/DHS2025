int suma(int a, int);

float division(int, int);

int suma(int a, int b) {
    int c = a + b;
    return c;
}

int main() {
    int x = 10, y = x, z = suma(x, y);
    int w = 3;
    z = division(w, x);
    return z;
}

float division(int a, int b){
    float c = a / b;
    return c;
}
